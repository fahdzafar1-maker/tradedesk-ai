# Screenshots

Put the four screenshots here, with exactly these names — the main README
links to them:

| File | What it shows |
|---|---|
| `01-infrastructure.png` | Railway project canvas: n8n + workers, its Postgres, the pgvector database, and the dashboard service |
| `02-injection.png` | Workflow 01 after a prompt-injection message — `escalation_agent` green, `intake_agent` untouched |
| `03-dedup.png` | Workflow 01 with a repeated `external_id` — routed to "Duplicate Ignored" |
| `04-dashboard.png` | The FastAPI owner dashboard |
